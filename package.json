{
  "name": "rectify",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "wrangler types && tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "wrangler": "^4.86.0"
  }
}
